#include <bits/stdc++.h>
using namespace std;

int main() {
    vector<int> v1 = {1, 2, 3, 4, 5};
    vector<int> v2(4);

    unordered_set<int> s(v2.begin(), v2.end());

    for (int x : v1) {
        if (!s.count(x)) {
            cou
        }
    }

}