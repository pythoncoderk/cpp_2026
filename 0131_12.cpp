#include <bits/stdc++.h>
using namespace std;

int main() {
    int a, b, c;
    cin >> a >> b >> c;

    if (a >= 20 && b >= 20 && c >= 20) cout << "OK" << "\n";
    else cout << "NG" << "\n";
    return 0;
}